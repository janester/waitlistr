-if @current_user.nil?
  =link_to("Login", login_path, :remote => true, :class => "button radius")
-else
  =link_to("Logout", logout_path, :method => "delete", :remote => true, :class => "button radius")